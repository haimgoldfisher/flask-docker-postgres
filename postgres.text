docker network create postgres_nw1

docker run --name functions_for_practice-postgres_server-1 -e POSTGRES_PASSWORD=start123 -d --network postgres_nw1  postgres

docker run -it --rm --network postgres_nw1 postgres psql -h functions_for_practice-postgres_server-1 -U postgres

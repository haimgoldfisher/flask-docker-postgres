docker network create postgres_nw1

docker run --name postgres_server1 -e POSTGRES_PASSWORD=start123 -d --network postgres_nw1  postgres

docker run -it --rm --network postgres_nw1 postgres psql -h postgres_server1 -U postgres
